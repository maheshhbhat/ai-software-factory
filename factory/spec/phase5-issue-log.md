# Phase 5 issue log

Working record for defects and environmental blockers discovered while running
Phase 5. Fixes remain local until Rung 1 is complete, then ship as one reviewed
batch. A finding is fixed during Rung 1 only when it prevents trustworthy Rung
1 evidence.

| ID | Finding and evidence | Rung 1 impact | Disposition |
|---|---|---|---|
| P5-001 | The readiness doctor did not exercise `git worktree add`, although delivery requires it before starting the engine. Project #422 — Phase 5 Rung 1 real /health KPI UAT failed at `2026-08-24T13:35:17Z` with `Operation not permitted` under `.git/worktrees`; Codex never started. | Blocking: preflight allowed a run that could not launch delivery. | Fixed by PR #426 — Block UAT when worker worktrees cannot be created. The doctor now creates and removes a temporary detached worktree. |
| P5-002 | The current Codex execution sandbox reports `.git` and `.git/worktrees` as owner-writable (`0755`, no ACL or flags), but the real Git operation returns `EPERM`. `codex login status` independently reports that PATH-alias creation is denied while authentication succeeds. | Environmental blocker: a valid black-box run cannot start from this execution context. | Open. Run preflight and UAT from a host context that permits `.git/worktrees` writes. Do not weaken the probe. |
| P5-003 | After process event `worker.outcome=NO_WORKER_LAUNCHED`, `rung1_live.py` watches only Project state and poller exit. The poller remains alive by design, so the harness waits until its 45-minute wall timeout after a terminal delivery failure. | Blocking for bounded, trustworthy failure evidence; delays diagnosis and obscures the first terminal cause. | Fixed locally. The harness stops on the durable terminal worker outcome for its Story without changing lifecycle state. |
| P5-004 | `rung1_report.build()` rejects every failed UAT before producing KPI availability results. The required final report must still enumerate all eight KPIs and mark measurements unavailable rather than fabricate them. | Blocking when Rung 1 verdict is FAIL or INCONCLUSIVE. | Fixed locally. Failed runs retain `black_box_uat.passed=false`, report all eight KPIs, and use explicit unavailable reasons. |
| P5-005 | Interrupting the Rung 1 harness raises `KeyboardInterrupt`, but its `main()` catches only `Exception`. Run `20260824T133419Z` therefore ended with a traceback and has no `evidence.json`, even though the base harness has an interruption-safe failure path. | Blocking: operator termination of a known-failed run does not preserve the promised frozen evidence bundle. | Fixed locally. Rung 1 now installs the base signal handler and freezes evidence on interrupt. Historical evidence remains unchanged. |
| P5-006 | The inherited `Run.fail()` posts `## Two-story E2E diagnostic` and points to `runs/two-story-real/...`, even when called by the Rung 1 harness. | Material evidence defect: a Rung 1 exception would publish the wrong evidence location and test identity. | Fixed locally. Rung 1 has its own failure recorder, diagnostic heading, and `runs/rung1/...` path. |
| P5-007 | The readiness doctor checks for a competing poller process but not competing recoverable work under Roadmap Commitment #384 — test-only factory verification. During run `20260824T154808Z`, the normal poll recovered and claimed old Story #425 — Add a `/health` endpoint returning the build SHA from Project #422 — Phase 5 Rung 1 real `/health` KPI UAT at `2026-08-24T15:49:01Z`, claimed new Story #429 — Add a `/health` endpoint returning the build SHA one second later, and launched Codex for the old Story. | Blocking: a supposedly isolated one-Story UAT can spend an engine call on preserved work from a prior failed Project and can never prove its one-Story criterion. | Fixed locally. The doctor now requires the selected test commitment to contain exactly the target Project and no pre-existing Story before creating fixtures. The harness passes that Project's actual commitment to both the doctor and `poll.sh`, and aborts on a durable dispatch naming any other Project or Story. Historical lifecycle state remains unchanged. |
| P5-008 | Repository inspection found no explicit invariant preventing a normally authorized factory-code Story from reaching an automated delivery worker. Existing factory planning records avoided dispatch because they lacked a ready lifecycle label, not because the production path forbade self-modification. This is the gap addressed by PR #433 — Plan the factory self-dispatch guard. | Blocking: a trustworthy Phase 5 measurement run cannot coexist with a production path that may use the factory to modify its own controls, contrary to the operating rule. | Fixed locally. One protected-surface classifier covers `factory/**`, all three root control scripts, GitHub and Claude controls, and root agent instructions. The dispatcher refuses before claim, the worker refuses before engine launch, and the merge gate refuses a marked worker artifact. Direct Codex implementation PRs remain permitted. Focused dispatcher, worker, and merge-gate suites pass. |
| P5-009 | Run `20260824T165751Z` for Project #435 — Phase 5 Rung 1 isolated `/health` KPI UAT dispatched only Story #436 — Rung 1: add a `/health` endpoint returning build SHA. Real Codex completed after 48.1 seconds and reported 124,490 input tokens plus 1,409 output tokens, but left the worktree unchanged. The worker correctly failed with `worker produced no repository changes`. Its successful engine output was consumed for usage accounting and discarded, so Codex's own explanation is unavailable from the frozen bundle. | The executed UAT is a valid FAIL. Missing engine output materially blocks root-cause diagnosis and makes another real-engine no-op unnecessarily opaque. | Fixed locally for future runs. A bounded credential-redacted tail of completed engine output is now written to the operation log. The historical bundle remains unchanged and the no-op is not reclassified. A fresh isolated UAT is still required. |
| P5-010 | `rung1_live.py start` captured the readiness doctor and printed no progress while preflight, fixture creation, polling and delivery ran. During run `20260824T165751Z`, the operator's terminal therefore appeared idle even though the factory was active and later failed. | Blocking for usable black-box operation: the required normal launch command gives the operator no indication whether it is checking dependencies, delivering, stalled or finished. The separate monitor remains useful, but basic launch status must not require a second unexplained command. | Fixed locally. The harness now prints the run ID, real-dependency preflight result, isolated commitment, created Story, each observed lifecycle state, final verdict, reason and evidence path. It still enters delivery only through `poll.sh`; no new control route or polling mechanism was added. |
| P5-011 | The Rung 1 Story target was always `runs/rung1/live_product/app.py`. PR #430 — Story #425 bounded delivery had already merged that file into `main`. In run `20260824T170541Z` for Project #438 — Phase 5 Rung 1 isolated `/health` KPI retry, Codex correctly reported that the existing scoped implementation and its three tests already satisfied Story #439 — Rung 1: add a `/health` endpoint returning build SHA, so it made no changes and the worker failed closed. The doctor checked queue isolation but not product-target freshness. | Blocking: a fresh Project and commitment can still request already-delivered work, making a valid black-box delivery impossible while preflight reports READY. | Fixed locally. Each Project now receives a distinct `runs/rung1/live_product/project-<project>/` scope. The harness passes the exact file target to the doctor, which reads live `main` through GitHub and refuses fixture creation if the target exists or cannot be checked. |
| P5-012 | Project #441 — Phase 5 Rung 1 fresh-target `/health` KPI UAT passed preflight and created Story #442 — Rung 1: add a `/health` endpoint returning build SHA, but the Story remained ready through repeated poll cycles. A read-only dispatcher evaluation showed it was eligible while global WIP was `2/2`: failed Story #436 and failed Story #439 were still claimed under older test commitments. The doctor treated any exit-zero `poll.sh --dry-run` as ready and did not interpret its WIP result. | Blocking: isolated commitment and target state do not guarantee delivery capacity. The harness can wait until timeout even though the exact Story cannot be selected. | Fixed locally. The doctor now requires a parsed WIP result with at least one free slot. After creating the fixture, the harness invokes the normal `poll.sh --once --dry-run` path and requires it to select exactly its one Story before starting the live poller. Historical lifecycle labels remain preserved; the separate failed-run teardown closes disposable issues without relabeling them. |
| P5-013 | Failed Rung 1 runs stopped their poller and froze evidence but left disposable Stories open with `story:claimed`. Story #436 and Story #439 then consumed both global WIP slots even though no worker process remained. Their different test commitments prevented the active isolated poller from reaching recovery before the next UAT. | Blocking: one correctly preserved failure can prevent every later isolated UAT from dispatching. Operator memory and manual label repair are not trustworthy cleanup controls. | Fixed locally in the test harness. After freezing a FAIL, it closes its own marker-verified disposable Story, any linked open worker PR, its verified Rung 1 Project, and its verified one-run commitment. It never removes lifecycle labels, returns work to ready, deletes history, or changes the verdict. Passing runs never use failed-run teardown. The doctor independently refuses full WIP, and the post-fixture dry-run requires exact selection. |
| P5-014 | The normal `poll.sh --once --dry-run` path captured and discarded the dispatcher's read-only report. Run `20260824T181740Z` for Project #444 — Phase 5 Rung 1 final clean-state `/health` KPI UAT therefore failed preflight because neither the doctor nor the harness could observe WIP capacity or exact Story selection through the required external entrypoint. | Blocking: semantic readiness cannot be proven through the black-box boundary, while directly invoking the dispatcher would violate that boundary. | Fixed locally. A dry poll now prints the dispatcher's unchanged read-only plan, including WIP and selected Stories. It makes no lifecycle write and creates no test-only control route. A deterministic poller test proves both the visible result and the no-write behavior. |
| P5-015 | During run `20260824T182917Z` for Project #446 — Phase 5 Rung 1 verified clean-state `/health` KPI UAT, the shared monitor repeatedly alternated between `ACTIVE` and `STUCK` during healthy Codex and reviewer calls. Its fixed fifteen-second stale threshold is shorter than the roughly thirty-second engine heartbeat interval. | Non-blocking for the UAT because later heartbeats and completed spans distinguish progress, but the warning is misleading. | Open for later. Do not alter a live successful run for a presentation defect. Calibrate the monitor threshold to the producer heartbeat after Rung 1 evidence is settled. |
| P5-016 | Project #446 — Phase 5 Rung 1 verified clean-state `/health` KPI UAT reached `project:awaiting-acceptance` with passing delivery evidence, but its approved checklist requires the final KPI report and successful-run commitment cleanup. The harness intentionally generated the final report and performed PASS cleanup only after a passing acceptance decision. | Blocking for a trustworthy acceptance verdict: the owner could not verify two criteria until after signing them, so marking all eight pass would have been circular. | Fixed locally after explicit owner authorization to preserve the successful run and correct the sequence. A pre-acceptance report now contains all eight KPIs, treats acceptance-dependent values as unavailable, and validates the evidence required at that point. The Project separates owner-signable evidence from mandatory post-acceptance cleanup and final reporting; either finalization failure still fails Rung 1. |
| P5-017 | The first pre-acceptance report for run `20260824T182917Z` counted only canonical touch-log bells. It omitted the operator's fixture launch and the explicit decision authorizing the acceptance-sequence correction, so it reported one human touch instead of three. | Blocking for a trustworthy human-touch KPI. A syntactically valid report that omits known human actions understates intervention. | Fixed locally. The report combines canonical bell receipts with classified run-specific operator actions, validates their shape, and keeps relay as a separate classification. The preserved run now reports three pre-acceptance touches and zero relay; final acceptance will add its own canonical receipt. |
| P5-018 | Direct campaign planning for Roadmap Commitment #17 — Phase 5 Rung 2 — required starting portfolio failed with `planning model failed (1)` and created no Project or Story. The normal `poll.sh` path loaded the dedicated Claude token, but `factory/agents/planning/run.sh` did not, so its direct Claude process had no authenticated identity. | Blocking for Rung 2 planning: the approved product scope cannot be turned into a Project for owner review. | Fixed locally. The direct planning wrapper now loads the same out-of-repository credential as the normal production wrapper, preserves an explicit caller token, emits no secret, and leaves a missing credential for the engine to report. Deterministic wrapper tests cover all three paths. |
| P5-019 | Campaign planning for Roadmap Commitment #17 — Phase 5 Rung 2 — required starting portfolio created Project #18 — Required starting portfolio for a chosen withdrawal plan with `project:awaiting-ready` and no Stories. Project expansion then failed closed with `trigger is not project:planning`. The campaign writer and its read-back check both encoded the premature lifecycle state. | Blocking for Rung 2 planning: the generated Project cannot receive Stories and therefore cannot become a complete approval artifact. | Fixed locally. New campaign Projects start in `project:planning`. An idempotent replay repairs only legacy Projects that are awaiting-ready while their Stories section is still `_No response_`; populated Projects are not moved backwards. Deterministic artifact tests cover creation, replay, and repair. |
| P5-020 | Project planning checks for `project:planning` only after the real model call and artifact write/read-back. The first Project #18 — Required starting portfolio for a chosen withdrawal plan expansion therefore created ADR #19 and Stories #20–#23, then reported failure because the trigger had prematurely been `project:awaiting-ready`. A later redundant invocation against the same awaiting-ready Project also entered the real model before the operator interrupted it. | Non-blocking for the current plan because the first invocation left a complete, verified approval artifact, but it can spend a model call and attempt writes for an ineligible Project before failing. | Open for later. Move the lifecycle precondition before the model invocation and prove an ineligible Project makes no engine call and no writes. Do not widen the blocking lifecycle repair PR. |
| P5-021 | After the owner requested a five-round verified-or-refuse design for Project #18 — Required starting portfolio for a chosen withdrawal plan, the planning revision updated ADR #19 and Stories #20–#23 but left the Project's `### Falsifiable acceptance criteria` unchanged. The unchanged checklist still promises that every valid input returns a portfolio, contradicting the new refusal outcome. The Project-output contract contains no field through which the planner can revise the owner-signable checklist. | Blocking for trustworthy Rung 2 plan approval: the approval receipt would bind the owner to criteria that contradict the detailed plan. | Fixed locally. Project-altitude output now requires an owner-signable acceptance checklist, the writer replaces that checklist in place before returning to approval, and read-back rejects a missing or malformed checklist. Deterministic tests prove requested criteria changes land without creating a new Project and empty criteria write nothing. |
| P5-022 | The normal dry poll for Roadmap Commitment #17 — Phase 5 Rung 2 — required starting portfolio proposed `story:blocked -> story:ready` for Stories #5 and #9 under unrelated Project #3 — Shadow a reproducible sequence-of-returns model beside the existing projection. Dispatch was commitment-scoped, but the sequencer planned across the entire repository. | Blocking for trustworthy Rung 2 evidence: a run for the approved commitment would mutate unrelated backlog lifecycle state before dispatch. | Fixed locally. The selected commitment is now passed through the poller into both Story readiness and Project completion sequencing. Deterministic tests with two commitments prove only the selected commitment advances while unscoped sequencer callers retain their existing behavior. |
| P5-023 | Live Rung 2 run `20260824T221020Z` selected Story #20 — Closed-form minimal portfolio in integer cents for fixed blended rates, but the worker tried to create its worktree from the local `ai-software-factory` checkout (`HEAD b7138bf`) and failed under `.git/worktrees` before Codex started. The worker's default checkout was the factory repository even though `--repo` named `maheshhbhat/income-portfolio-analyzer`; it never verified the match. | Blocking for Rung 2 delivery: no product-repository Story can launch from the normal poll path, and the failed claim consumed Attempt 1. | Fixed locally. The worker reuses a configured checkout only when its GitHub origin matches the requested repository; otherwise it creates a temporary authenticated checkout of the requested product, then creates the bounded Story worktree there. Tests cover reuse, mismatch cloning, clone failure, and secret-free clone arguments. Normal lease recovery, not manual relabeling, will retry Story #20. |
| P5-024 | A worker that exited non-zero with a confirmed `FAILED` result left Story #20 — Closed-form minimal portfolio in integer cents for fixed blended rates claimed for the full sixty-minute ambiguity lease. The runtime had no bounded operator repair mode for a claim stranded by a known factory malfunction. | Blocking for operating velocity and recovery: the safety lease is necessary for unknown outcomes, but wrong for a process proven stopped. | Fixed locally. A directly observed non-zero exit releases the claim immediately while preserving the failed Attempt; ambiguous outcomes retain the lease. A guarded operator repair command accepts only matching failed-worker evidence, refuses a linked delivery PR, records the intervention on the Story, and cannot count as autonomous delivery. |
| P5-025 | The repaired Rung 2 retry launched nested Codex and failed immediately with `failed to initialize in-process app-server client: Operation not permitted`. The old doctor checked login metadata but never started the configured delivery engine. | Blocking: a doctor could report READY even though the worker engine could not execute in the production environment. | Fixed in this change. The doctor starts the configured real worker with a harmless read-only prompt and requires its exact answer. A real Claude worker probe succeeded; deterministic tests prove a launch failure blocks readiness. |
| P5-026 | During the Claude delivery attempt for Story #20 — Closed-form minimal portfolio in integer cents for fixed blended rates, worker and reviewer engine output was captured until process exit. The shared monitor also refused to render a normal Rung 2 poller run without harness `run-state.json`. | Blocking for trustworthy operation: the operator could not distinguish useful engine progress from a hung process. | Fixed in this change. Worker, reviewer, and planning engines emit bounded credential-redacted line progress; ordinary components retain activity heartbeats. The monitor renders observability-only production runs and waits forty-five seconds before declaring a missed thirty-second heartbeat stuck. A real Claude stream produced ten visible lifecycle records before completion. |
| P5-027 | The Claude worker completed implementation for Story #20 — Closed-form minimal portfolio in integer cents for fixed blended rates, then the wrapper ran the factory's Python test script inside the JavaScript product checkout. The valid product change was discarded and Attempt 3 became poisoned. | Blocking: a product delivery cannot pass if validation is hard-coded to the factory repository. | Fixed in this change. Validation is selected from the checked-out product: an explicit operator command wins, a declared JavaScript test script uses `npm test`, the factory uses its own wrapper, and an unknown repository fails closed. Deterministic tests cover every branch. |
| P5-028 | `poll.sh` loaded the dedicated reviewer OAuth token into the shared process environment. Selecting Claude for delivery would therefore make the worker use the reviewer's identity unless the operator supplied a special launch override to unset it. | Blocking: the recovery route would violate fresh independent review and require manual launch glue. | Fixed in this change. The reviewer reads `.factory-reviewer-token` only inside its fresh identity environment; the shared poller removes that token before worker launch. Deterministic wrapper tests prove file and caller tokens cannot reach delivery, while the real normal Claude worker still starts from its own login. |
